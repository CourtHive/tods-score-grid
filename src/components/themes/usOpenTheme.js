import { createTheme } from './createTheme';

export const usOpenTheme = createTheme('usopen-theme', {
  colors: {
    connector: '#999',
    borderHover: '#0091d2'
  },
  matchUp: {},
  participant: {}
});
